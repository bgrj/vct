# 执行指令 · 逐集生成《大都市的爱情法 / Love in the Big City》视频截图

> **本文件是任务书**：交给 AI 助手（CodeBuddy / Codex / Cursor / VSCode 内 AI 等）即可直接执行。
> 用户只需把本文件路径发给 AI，再说一句：**「按 执行指令.md 执行第 XX 集」**。
> 角色分工：**AI 助手 = 执行人**（跑工具、抽帧、自查、汇报）；**用户 = 验收人**（逐集人工确认）。
>
> 版本：v2.0　｜　更新日期：2026-09-11　｜　取代同目录 `AI执行指令_逐集视频截图.md` 的内容
> 配套文档：`本地视频自动化截图工具使用指南.md`（小白流程）、`README.md`（参数与实现细节）

---

## 0. 占位符约定（先读这一段）

本文件里的路径已按**本机实际情况**填好。**换机器 / 换用户时，只需替换下表左列的路径**，其余规则不用改。

| 占位符 | 本机实际值（**可替换成你自己实际的情况**） | 说明 |
| --- | --- | --- |
| `【工具目录】` | `D:\03_Knowledge\intimate\Video Cut` | 截图工具本体所在目录（AI 应以它作为工作区） |
| `【素材目录】` | `D:\03_Knowledge\intimate\Love in the big City` | 剧集素材根目录，`01`~`08` 每集一个文件夹 |
| `【Python】` | `E:\Tools\Python312\python.exe` | 必须是 Python 3.12（3.13 人脸库不稳定） |
| `【FFmpeg】` | `E:\Tools\ffmpeg\bin\ffmpeg.exe` | 程序会自动在这些目录找：`E:\Tools\ffmpeg\bin` → `C:\ffmpeg\bin` → `D:\ffmpeg\bin` → `C:\Program Files\ffmpeg\bin` → `D:\Program Files\ffmpeg\bin` → 系统 PATH |
| `【集号】` | `01` ~ `08` | 两位数字，与素材目录名一致 |
| `【剧集文件】` | `Love.in.the.Big.City.S01EXX.2024.1080p.WEB-DL.H264.AAC-ZeroTV.mkv` | 每集目录内只有一个 `.mkv` |

> 若你换了路径：本文件所有出现上述路径的位置，按实际替换即可；**文件名与目录结构规则不要改**（工具按「目录名 + `-pc`」自动生成输出目录）。

---

## 1. 执行目标（一句话）

**打开【工具目录】下的视频截图工具 → 读取【素材目录】下某一集的视频 → 为该集逐句台词生成视频截图（1920×1080，保留原字幕观感）→ 落入该集对应的 `XX-pc` 输出目录 → 汇报并等待用户确认 → 确认通过后才做下一集。**

- 一次只做**一集**，跑完停下等确认（**严禁**自动连跑下一集）。
- 集内按台词时间顺序处理；集间按 `01 → 02 → … → 08` 顺序处理。
- 除 AI 助手本身需要联网外，**截图处理全程离线在本机完成**（字幕取自视频内嵌简中软字幕轨，人脸与画质分析在本机推理，视频与图片不上传）。

---

## 2. 截图画面要求（用户硬性要求，逐条执行）

### 2.1 分辨率与格式

- **分辨率：1920×1080**（原片完整画幅，工具按原帧输出，**不裁剪、不缩放、不加边框**；本剧集片源即 1080p）。
  若输入片源不是 1080p，**必须在汇报中说明实际分辨率**，不得谎报。
- **格式：JPG**（工具默认，质量 95，扩展名 `.jpg`）。
  如需 **PNG**：属源码级开关——把 `【工具目录】\video_tool\config.py` 中的 `IMAGE_FORMAT = "jpg"` 改成 `"png"`；改完对该集加 `--clean` 整集重建，避免同一目录 JPG / PNG 混装。

### 2.2 命名规则（必须满足：`集数_时间码_场景描述`）

- **三要素齐全、顺序一致**：`集数` → `时间码` → `场景描述`。
- `集数`：取自视频文件名中的 `SxxExx`（例：`S01E04`）；文件名里没有时回退用视频主名。
- `时间码`：画面所在时间点，格式 `HH-MM-SS.mmm`（例 `00-31-12.500`）。**必须可被 `ffmpeg -ss` 直接复用**。
- `场景描述`：中文短语，8~20 字，概括该句台词或该画面的情境（例：`高煐翻看夜店合照`）。**只允许中文、字母、数字、`-`**，不得出现空格与 `\ / : * ? " < > |`。
- 两种落地形态（二选一，**同一集内部必须统一**）：
  - **推荐形态（工具原生命名 + 描述后缀，可断点续跑）**：
    `S01E04_0131_00-31-12.500_高煐翻看夜店合照.jpg`
    （集数 → 四位句序 → 时间码 → 场景描述；句序段仅为排序与续跑服务，不破坏三要素）
  - **严格形态（逐字符合用户命名规则，去掉句序段）**：
    `S01E04_00-31-12.500_高煐翻看夜店合照.jpg`
    ⚠️ 严格形态会**破坏工具的断点续跑**（工具靠 `<集号>_<四位句序>_` 前缀判断已生成）。因此：① 必须在整集跑完、确认无误后再批量改名；② `_index.csv` 必须保留「句序 ↔ 文件名」对应关系；③ 该集日后若需重跑，必须加 `--clean`。

### 2.3 关键帧选取标准（**最高优先级**）

**深度理解场景与台词的核心表达意图**，而不是机械地「有字幕就截」：

1. **优先截取台词出现时的画面**——画面与台词同时成立（工具已按此逻辑在每句时间区间内采样候选帧并选优）。
2. **必须截取承载前文情感或剧情延续的关键画面**——即**没有台词、但有意义**的镜头。
   - **典型范例（第 4 集）**：高煐妈妈在病床上让高煐把旁边抽屉里黄色的东西拿给她，
     **之后画面停留在高煐此前与卢英洙万圣节在夜店的合照上**——这张图**没有任何台词**，
     但它承接「妈妈的嘱托 → 高煐对旧情的回忆」，属于**必须截取**的剧情延续帧。
   - 同类画面还包括但不限于：信物 / 遗物特写、两人合照、空荡的房间、雨夜背影、渐隐黑场前的最后一帧、人物反应镜头（沉默、落泪、回望）。
3. 这类**无台词承载帧必须被主动识别并单独补截**，做法见第 6 节；**补截清单必须在汇报中逐条列出**（时间码 + 画面内容 + 为什么必须截）。

### 2.4 人物状态要求

- **眼睛状态、说话状态必须清晰**：优先「双眼睁开、开合对称」；说话状态为「正在自然说话」。
- **允许夸张的面部表情**：大笑、惊讶、哭腔、瞪眼等有戏的表情可以保留。
- **必须避开**：闭眼 / 半闭眼 / 眨眼瞬间（一眼睁一眼眯）/ 嘴部扭曲 / 张口过大。
- 工具已内建「闭眼帧重采样」自动补救：整句候选都闭眼时自动把候选帧数与采样密度翻倍再挑一次。
- 允许出现极少数「整句候选都没睁开眼」的图（多为低头、闭眼喝水、闭眼摇头等剧情动作）——**在汇报中列为遗留问题**，不得隐瞒。

### 2.5 画面质量

- 必须**清晰、无运动模糊**；工具以拉普拉斯方差设硬门槛，模糊帧直接淘汰、不参与优选。
- 无人像的空镜、背影、远景、字幕卡**照常出图**（属正常保留，报告标注为「降级」，本剧集约 4~5 成，不是失败）。

### 2.6 字幕与水印

- **保留原字幕**：输出图 = 原片画面 + 该句台词文字（白色粗体 + 黑色描边、无底框、水平居中、贴近画面底部、最多 3 行）。
  本片源为软字幕（画面本身不带烧入字幕），工具把该句台词渲染上去即为「保留原字幕观感」，样式基线取自参考素材 `素材3.0-3.2图片`（命中 230 张样本）。
- **不加任何额外水印**、不加半透明黑底、不加边框、不加台标。
- **无台词补截图**：保持原片画面原样，**不叠加任何文字**。

### 2.7 剧情关联

- 每张图要能体现「**当前台词**」或「**前文情节的延伸**」。
- **交付前必须反复确认：无台词但有情感/剧情承载意义的画面没有被漏掉**（对应第 7 节核对清单第 4 条）。

---

## 3. 输出路径与文件夹结构

**规则**：输出目录 = 视频所在目录名 + `-pc`，位于该集目录内（**无需手工创建**，首次运行自动生成）。

```text
D:\03_Knowledge\intimate\           ← 【素材目录】的上一级（可替换成你实际的位置）
├── Video Cut\                      ← 【工具目录】：工具本体（main.py / video_tool / models / style_profile.json）
└── Love in the big City\           ← 【素材目录】
    ├── 01\
    │   ├── Love.in.the.Big.City.S01E01.2024.1080p.WEB-DL.H264.AAC-ZeroTV.mkv   ← 输入视频
    │   └── 01-pc\                  ← 输出目录（跑完自动出现）
    │       ├── S01E01_0001_00-00-05.880_<场景描述>.jpg   ← 每句台词一张图，按文件名排序即台词顺序
    │       ├── S01E01_X01_00-31-12.500_<场景描述>.jpg    ← 无台词剧情延续帧（补截，`X` 序号不占用数字句序）
    │       ├── _index.csv          ← 台词索引表（utf-8-sig，Excel 可直接打开）
    │       └── _report.txt         ← 处理报告（句数 / 成功 / 降级 / 失败 / 缺失 / 眼睛统计 / 验收结论）
    ├── 02\02-pc\  …  07\07-pc\     ← 每集一套，结构完全一致
    ├── 08\08-pc\                   ← 当前未完成（见第 9 节进度台账）
    └── 素材3.0-3.2图片\              ← 参考素材（**只读**：字幕样式与构图基线来源，禁止增删改）
```

**每个 `XX-pc` 目录内应有且仅有三类文件**：`*.jpg`（截图）、`_index.csv`（台账）、`_report.txt`（报告）。
临时抽帧目录（若用到）必须建在系统临时目录或本集目录下的 `_tmp_story\`，**用完立即删除**，不得留在交付目录里。

---

## 4. 执行顺序（严格串行：**先打开工具 → 再读取视频 → 最后输出截图**）

**总原则**：按 `01 → 02 → … → 08` 顺序；**一集做完停下等用户确认，确认通过才做下一集**（不并行、不交叉、不提前跑下一集）。

每一集固定五步：

### ① 打开工具（环境自检）

```bat
cd /d "D:\03_Knowledge\intimate\Video Cut"
"E:\Tools\Python312\python.exe" -c "import cv2, numpy, PIL, mediapipe; print('deps ok')"
"E:\Tools\Python312\python.exe" main.py --help
```

- 必须确认齐备：`main.py`、`video_tool\`、`models\face_landmarker.task`、`style_profile.json`。
- `--help` 能打印参数说明即说明 Python、依赖、脚本正常。
- **PowerShell 差异**：把 `cd /d "路径"` 换成 `Set-Location "路径"`；行首是带引号的 exe 路径时，前面加 `& `（例如 `& "E:\Tools\Python312\python.exe" main.py --help`）。

### ② 读取该集视频（先看片源状态）

```bat
dir "D:\03_Knowledge\intimate\Love in the big City\08"
```

- 目录内应有 1 个 `.mkv`（本剧集 7 个集数约为 2.7~3.1 GB）。
- 若存在 `.qkdownloading` / `.part` / `.crdownload` 等**未下载完成**的文件：工具会自动跳过并在报告中说明；**此时不要出图**，先告知用户等下载完整。
- 确认字幕轨：本片源为 `0:2「简体中文」(subrip)` 文本型软字幕；若报「找不到字幕 / 没有台词」，说明片源无文本型软字幕，**停下来报告用户换片源**（工具不做 OCR）。

### ③ 输出截图（先 3 句试跑 → 再整集重建）

```bat
REM 3.1 试跑前 3 句（新集 / 改过样式时必做）
"E:\Tools\Python312\python.exe" -u main.py --input "D:\03_Knowledge\intimate\Love in the big City\08" --max-cues 3

REM 3.2 目视确认这 3 张（样式、睁眼、清晰），确认后**删掉这 3 张**，再跑整集

REM 3.3 整集出图（--clean 先清空该集历史图片与旧索引，避免新旧样式混装）
"E:\Tools\Python312\python.exe" -u main.py --input "D:\03_Knowledge\intimate\Love in the big City\08" --clean
```

- 一集约 200~350 秒（300~500 句），**必须后台运行 + 日志落盘**，否则会一直卡住等待：

```bat
set PYTHONIOENCODING=utf-8
"E:\Tools\Python312\python.exe" -u main.py --input "D:\03_Knowledge\intimate\Love in the big City\08" --clean > "_run08.out.log" 2> "_run08.err.log"
```

- **踩过的坑（必须避免）**：素材路径含空格（`Love in the big City`），`--input` 的值**必须整段加双引号**。
  曾因未加引号报错 `main.py: error: unrecognized arguments: in the big City\08`（见 `_run08.err.log`），第 08 集因此中断在第 54 句。
- 半闭眼偏多时可加 `--eye-refine half`（半闭句也重采样，耗时略增）。
- 中断/补跑：不加 `--clean` 直接重跑即**断点续跑**（已生成的图片自动跳过，并补回索引表）。

### ④ 自检（三量对齐，缺一不可）

```bat
REM 图片张数
powershell -NoProfile -Command "(Get-ChildItem 'D:\03_Knowledge\intimate\Love in the big City\08\08-pc' -Filter *.jpg).Count"
```

必须同时满足（数字三方相等）：

```text
图片张数 = `_report.txt` 的「台词总句数」 = `_index.csv` 有效行数（不含表头）
```

并且 `_report.txt` 中：`处理失败` = 0、`完整性校验 → 发现缺失` = 0。
不满足就先补跑（重跑该集，工具会自动补缺失句），**不得带病汇报**。

### ⑤ 汇报并**停下等确认**

按第 8 节模板汇报；**用户明确回复「通过/继续」后，才进入下一集**。

> 注意：个别集数受剧情动作影响会出现 0~5 张闭眼帧（如低头、闭眼喝水、闭眼摇头），
> 此时报告结论会写「基本通过（…建议目检）」——**这不等于失败**，但必须在汇报中显式列出并说明原因。

---

## 5. 命令行速查（本工具全部参数）

| 参数 | 说明 | 默认 |
| --- | --- | --- |
| `--input "路径"` | 输入目录（**台词模式传单集目录**，必须加引号）；缺省则打开图形界面 | 无 |
| `--mode dialogue\|uniform` | 台词智能截图 / 均匀截图 | `dialogue` |
| `--subtitle-track auto` | 字幕轨，`auto` 优先简体中文，也可传轨名关键字 | `auto` |
| `--candidate-max 10` | 每句最多采样候选帧数（半闭眼偏多可提到 15） | `10` |
| `--start-cue N` / `--max-cues N` | 从第 N 句开始 / 只处理前 N 句（`0` = 全部） | `1` / `0` |
| `--no-subtitle-text` | 不在图片上渲染台词文字 | 关闭 |
| `--no-resume` | 不跳过已存在图片（重新生成） | 关闭 |
| `--clean` | **重建**：先清空该集历史图片与旧索引再生成 | 关闭 |
| `--eye-refine closed\|half\|off` | 闭眼重采样口径：整句闭眼才救 / 半闭也救 / 关闭 | `closed` |

---

## 6. 特殊要求：无台词「剧情延续帧」的识别与补截

工具默认逻辑是「**一句台词一张图**」，**不会主动截取无台词的承载帧**（如第 4 集的万圣节夜店合照）。
因此**每一集交付前，AI 必须独立做一轮剧情延续帧排查**——这是本任务与普通截图最大的区别。

### 6.1 排查流程

1. **读台账梳理剧情**：读该集 `_index.csv` 的「台词」列，按时间顺序梳理本集的人物、事件、时间线。
2. **找「疑似无台词空窗」**：在台词时间线上找**相邻两句间隔较大**（建议阈值 **≥ 6 秒**）的区间，以及**每场戏结尾、转场、渐隐**处——这些位置最容易藏着无台词承载帧。
3. **抽候选帧排查**（用 ffmpeg 精确抽帧到临时目录）：

```bat
REM 例如排查 00:31:00 ~ 00:31:30 之间的无台词画面（每 2 秒 1 帧）
mkdir "_tmp_story"
"E:\Tools\ffmpeg\bin\ffmpeg.exe" -y -ss 00:31:00 -i "D:\03_Knowledge\intimate\Love in the big City\04\Love.in.the.Big.City.S01E04.2024.1080p.WEB-DL.H264.AAC-ZeroTV.mkv" -t 30 -vf fps=2 -q:v 2 "_tmp_story\frame_%04d.jpg"
```

4. **AI 逐张读图判断语义**（AI 可直接读取图片）：判断它是否承载前文情感 / 回忆 / 信物 / 剧情延续。
5. **选定 1 帧后精确定帧导出**（`-ss` 放在 `-i` **之后**为精确定位到帧）：

```bat
"E:\Tools\ffmpeg\bin\ffmpeg.exe" -y -ss 00:31:12.500 -i "D:\03_Knowledge\intimate\Love in the big City\04\Love.in.the.Big.City.S01E04.2024.1080p.WEB-DL.H264.AAC-ZeroTV.mkv" -frames:v 1 -q:v 2 "D:\03_Knowledge\intimate\Love in the big City\04\04-pc\S01E04_X01_00-31-12.500_高煐翻看夜店合照.jpg"
```

6. **清理临时目录**：`_tmp_story\` 用完立即删除，不得留在交付目录。
7. **记录到汇报**：清单格式 = 时间码 + 画面内容 + 为什么必须截。

### 6.2 必须排查的典型承载帧类型

| 类型 | 例子 | 是否必截 |
| --- | --- | --- |
| 回忆/信物特写 | 第 4 集万圣节夜店合照、抽屉里的黄色物件、旧照片、票据 | **必截** |
| 人物反应镜头 | 台词后人物沉默、落泪、回望、握拳、长吸气 | **必截** |
| 场景空镜 | 空荡的房间、雨夜背影、渐隐黑场前的最后一帧 | 视剧情承载而定 |
| 纯黑过场 / 字幕卡 | 仅底部有文字的过场 | 一般不补 |

### 6.3 剧情线索（用户提供，AI 处理时先加载）

- **高煐**：本剧主角。
- **卢英洙**：与高煐有重要情感关联的人物（万圣节夜店合照的另一方）。
- **高煐妈妈**：第 4 集病中嘱托高煐「把旁边抽屉里黄色的东西拿给她」→ 随后画面停留在高煐与卢英洙的万圣节夜店合照上。
- AI 每集处理**前**，先输出一段「**本集剧情脉络 + 预判需补截清单**」给用户；处理**后**再补上实际补截清单。**除用户要求外，不额外落盘新文件**。

---

## 7. 单集交付前核对清单（AI 逐条打勾后才能汇报）

- [ ] 1. **三量对齐**：`图片张数 = 台词总句数 = _index.csv 有效行数`；`处理失败 = 0`；`发现缺失 = 0`
- [ ] 2. **命名合规**：抽查 3 个文件名，均满足 `集数_时间码_场景描述`，字符合法、无空格、同集风格统一
- [ ] 3. **样式一致**：抽查 2~3 张，白字粗体 + 黑描边、水平居中、贴底、最多 3 行；**无额外水印、无半透明黑底**
- [ ] 4. **剧情延续帧已排查**：无台词但承载情感/剧情的画面已补截（`X` 序号命名），清单已列出
- [ ] 5. **画质与人物状态**：抽查「有人脸且眼睛睁开」的图清晰不糊；「无人像」空镜/字幕卡同样清晰保留；闭眼帧已列为遗留问题
- [ ] 6. **降级说明**：降级图（无人像/平坦画面）占比约 4~5 成属正常，已在汇报中说明
- [ ] 7. **报告回读**：`_report.txt` 的字幕轨、样式基线来源、降级原因、错误列表均无异常
- [ ] 8. **目录干净**：`XX-pc` 内只有 `*.jpg` / `_index.csv` / `_report.txt`；临时抽帧目录与临时日志已清理

---

## 8. 汇报模板（每集跑完按此格式提交，然后停下等确认）

```text
集号：第 08 集　｜　产物目录：D:\03_Knowledge\intimate\Love in the big City\08\08-pc
视频文件：Love.in.the.Big.City.S01E08....mkv　｜　字幕轨：0:2「简体中文」(subrip)
台词总句数：XXX　｜　图片数：XXX　｜　索引行数：XXX　（三量一致 ✅ / ❌）
成功出图：XXX　｜　降级：XXX（无人像 XX / 平坦画面 XX）　｜　失败：0
眼睛状态：睁开 XX　｜　半闭 XX　｜　闭合 XX（多为剧情动作，建议目检）
分辨率/格式：1920×1080 / JPG
命名形态：推荐形态（集数_句序_时间码_场景描述）
样式基线：style_profile.json（素材3.0，230 张命中样本）
剧情延续帧补截：X 张，清单如下：
  1) 00-31-12.500 高煐与卢英洙万圣节夜店合照 —— 承接妈妈嘱托后对旧情的回忆，无台词但必须截
遗留问题：无 / （列出闭眼帧句序、失败句、未下载完成文件等）
请确认本集是否通过；通过后我再开始第 09 集（或按你的指示处理）。
```

---

## 9. 当前进度台账（真实数据，AI 开工前先核对一遍）

| 集 | 输入视频 | 输出目录 | 图片数 | 台词句数 | 报告 | 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| 01 | ✅ | `01\01-pc` | 525 | 525 | 基本通过（闭眼 3） | 已生成 |
| 02 | ✅ | `02\02-pc` | 460 | 460 | 基本通过（闭眼 5） | 已生成 |
| 03 | ✅ | `03\03-pc` | 390 | 390 | 通过 | 已生成（旧版索引，眼睛列为「是/否」） |
| 04 | ✅ | `04\04-pc` | 353 | 353 | 通过 | 已生成（旧版索引） |
| 05 | ✅ | `05\05-pc` | 373 | 373 | 基本通过（闭眼 2） | 已生成 |
| 06 | ✅ | `06\06-pc` | 346 | 346 | 基本通过（闭眼 2） | 已生成 |
| 07 | ✅ | `07\07-pc` | 324 | 324 | 通过 | 已生成 |
| 08 | ✅（2.9 GB，已下载完整） | `08\08-pc` | **54（中断）** | 未出报告 | **无 `_index.csv` / `_report.txt`** | **待补跑：上次因 `--input` 未加引号报错中断在第 54 句** |

**下一步待办**：

1. **优先补跑第 08 集**：该集已是同批次最新样式，直接续跑即可补齐（不加 `--clean`）：
   ```bat
   cd /d "D:\03_Knowledge\intimate\Video Cut"
   set PYTHONIOENCODING=utf-8
   "E:\Tools\Python312\python.exe" -u main.py --input "D:\03_Knowledge\intimate\Love in the big City\08" > "_run08.out.log" 2> "_run08.err.log"
   ```
   （若发现样式有变，改用 `--clean` 整集重建。）
2. 第 08 集跑完后，逐集做**剧情延续帧排查**（第 6 节）。
3. 第 03、04 集若需要「睁开/半闭/闭合」三态眼睛统计，可在用户同意后加 `--clean` 重跑一次。
4. 全部集数完成后，可清理过程日志 `_run*.out.log` / `_run*.err.log`。

---

## 10. 红线（禁止事项）

1. **不得联网处理素材**：不使用任何在线 API / 云服务 / 在线 OCR / 在线翻译（AI 助手自身除外）。
2. **不得修改素材目录**：`素材3.0-3.2图片` **只读**；不得增删改任何原始素材与视频文件。
3. **不得跨集并行**：未经用户确认，不得同时跑两集，也不得提前跑下一集。
4. **不得跳过自检**：三量对齐与抽样目视未通过前，不得汇报「已完成」。
5. **不得默认丢图**：任何一句台词缺配图都必须先补跑，补跑仍失败要在报告中显式说明。
6. **不得残留旧图**：变更样式 / 格式后必须 `--clean` 重建，避免新旧规格混在同一目录。
7. **不得删除已验收产物**：已确认通过的集数目录，只允许追加（补截图），不得整体清空或删除。
8. **不得隐藏问题**：闭眼帧、失败句、未下载完成的文件、分辨率不符等，一律如实写进汇报。
9. **不得漏掉无台词承载帧**：第 6 节的排查是**交付必做项**，不是可选项。
10. **路径必须加引号**：所有含空格路径（`Love in the big City`）在命令中必须整段加双引号。

---

## 11. 常见问题速查

| 现象 | 原因 | 处理 |
| --- | --- | --- |
| `error: unrecognized arguments: in the big City\08` | `--input` 路径未加引号，被空格截断 | 给路径加双引号后重跑（断点续跑，不会重做已完成部分） |
| 报「找不到字幕 / 没有台词」 | 片源没有文本型软字幕（只有硬字幕或图形字幕 PGS/VobSub） | 停下报告用户换片源；本工具不做 OCR |
| 报告写「跳过未下载完成」 | 目录里有 `.qkdownloading` / `.part` | 等下载完整再跑 |
| 图片数比台词数少 | 极少数句子处理失败 | 直接重跑该集，工具自动补跑缺失句 |
| 换过样式后新旧图混装 | 历史图片未清 | 加 `--clean` 重跑该集 |
| 中文显示成方块 | 系统缺中文字体 | 装微软雅黑（优先 `C:\Windows\Fonts\msyhbd.ttc`）或把 `msyh.ttc` 放入 `C:\Windows\Fonts` |
| 满屏 WARNING 黄字 | 人脸库的正常噪音 | 忽略，只看报告汇总 |
| 半闭眼偏多 | 候选帧抓得太少 | 加 `--candidate-max 15` 或 `--eye-refine half` 重跑该集 |
| 看不到实时进度 | 后台运行 | 看输出目录图片数量是否在增加，或看 `_run08.out.log` |

---

## 12. 直接复制给 AI 的开场指令（用户用）

**补跑第 08 集（最常用）：**

```text
请打开工作区 D:\03_Knowledge\intimate\Video Cut，
严格按同目录 执行指令.md 执行第 08 集：
先跑依赖自检 → 读取 D:\03_Knowledge\intimate\Love in the big City\08 的视频（注意 --input 路径必须加引号）
→ 断点续跑补齐该集截图（不加 --clean）→ 完成三量对齐自检
→ 做一轮无台词「剧情延续帧」排查并按 集数_时间码_场景描述 命名补截
→ 按汇报模板报给我，然后停下等我确认，不要自动继续。
```

**处理新的一集（例：第 09 集）：**

```text
请打开工作区 D:\03_Knowledge\intimate\Video Cut，严格按 执行指令.md 执行第 09 集：
先打开工具做环境自检 → 读取该集视频 → 先跑 3 句给我目视确认（确认后删掉这 3 张）
→ 再整集 --clean 出图 → 三量对齐自检 → 剧情延续帧排查与补截
→ 按模板汇报并停下等我确认。
```
