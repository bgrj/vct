# 本地视频自动化截图工具 · 小白使用指南

**一句话说明**：把一集带中文字幕的视频交给它，它会自动帮你「**每有一句台词，就截一张图**」，
把这句话以统一的样式印在画面底部，并按顺序命名保存好。

- 工具本体放在：`D:\03_Knowledge\intimate\Video Cut`
- 剧集素材放在：`D:\03_Knowledge\intimate\Love in the big City`（每集一个文件夹）

> 怎么读这份文档：
> 先看 **第 1、2 节**（它是干嘛的）→ 再看 **第 3 节**（它做不到什么，很重要）→
> 然后 **第 4 节** 看整体路线 → 照着 **第 7 节** 一步步做 → 结果用 **第 9 节** 检查 →
> 遇到问题查 **第 10 节**，看不懂的词查 **第 11 节**。

---

## 1. 这个工具帮你做什么（大白话版）

假设你要整理一部剧的资料，需要「**每到一句台词，就留一张画面**」。

手动做的痛点很现实：

| 手动做 | 实际结果 |
| --- | --- |
| 一集几百句台词，一句句拖进度条截图 | 做不完，一盘剧要几十小时 |
| 随手暂停就截 | 经常截到眨眼、张嘴、糊帧 |
| 画面和台词对不上 | 回头看不出这张图对应哪句话 |
| 不同时间截的图 | 字号、位置、底色都不一样，没法直接排版 |

这个工具把上面的活全自动做掉：

1. **读字幕**：从视频里自带的软字幕中取出每句台词的开始/结束时间（不靠识别、没有错字）
2. **抓画面**：在每句台词的时间段里抓好几张候选画面（默认 5~10 张）
3. **挑最好的一张**：自动比清晰度、人物神态、画面构图，挑出最自然的一张
4. **印台词**：把该句台词以「白字 + 黑边、水平居中、贴近画面底部」的样式印上去
5. **按顺序存好**：命名成 `集号_第几句_时间码.jpg`，并生成一张索引表和一个处理报告

**重点（眼睛比嘴巴更重要）**：挑画面时，它会**优先看人物眼睛**——
眼睛得是睁开的、两只眼开合差不多（不是一只睁一只闭），
其次才看嘴巴是不是正在说话的自然口型。
所以它既避开「闭眼、半闭眼、眨眼瞬间」，也避开「张着血盆大口」的怪表情。

## 2. 做出来的是什么（先有心理预期）

每跑完一集，你会得到一个文件夹，例如 `05\05-pc\`，里面有三样东西：

| 文件名 | 是什么 | 怎么看 |
| --- | --- | --- |
| `S01E05_0007_00-12-34.567.jpg` | 第 7 句台词对应的画面，台词印在底部 | 直接双击打开看，按文件名排序就是台词顺序 |
| `_index.csv` | 索引表（台账） | 用 Excel 打开，一句一行，含台词、文件名、**眼睛状态**、说话状态、是否降级 |
| `_report.txt` | 处理报告（成绩单） | 记事本打开，看「总句数 / 成功 / 失败 / 缺失 / 眼睛统计 / 验收结论」 |

## 3. 它做不到什么（先说清楚，省得白费功夫）

| 它做不到 | 原因 | 你该怎么办 |
| --- | --- | --- |
| 视频没有字幕时无法出图 | 它的台词来自视频自带字幕，不是听声音 | 换带中文字幕的片源，或用别的方式先加字幕 |
| 画面里「烧死」的字幕（硬字幕）读不到 | 硬字幕是图像的一部分，需要 OCR，本工具不做 | 只能换软字幕片源 |
| 只有图形字幕（PGS/VobSub）的片源读不到 | 这类字幕不是文字格式 | 换软字幕片源 |
| 无人物的空镜、物品特写不会有人脸判断 | 画面里没有人，就没有眼睛/嘴巴可判断 | 这是正常的，报告里记为「无人像」，图照样出 |
| 用 AI 助手指挥时需要联网 | AI 助手本身在云端 | 见第 4 节：先联网跑通，再改成离线一键运行 |
| 首次安装需要联网 | 要下载 Python、FFmpeg、依赖包 | 装一次就够，以后可以完全离线 |
| 不能保证 100% 每张图都完美 | 它是「挑相对最好的一张」，不是人工精修 | 报告里会给出统计，按第 9 节抽查即可 |

**关于「联网」的准确说法**：

- **处理视频这一步，永远在你本机做**，视频和图片不会被上传；
- **联网只发生在**：① 装软件/依赖那一次；② 用 AI 助手（CodeBuddy 等）指挥、看结果的时候；③ 万一需要重新下载人脸模型文件。
- 所以：**不用 AI 助手、只双击脚本的方式，可以完全不联网**（第 8 节会教你怎么做）。

## 4. 整体路线：先用「有人帮忙」的方式跑通，再做成「不用网也能跑」

| 阶段 | 要联网吗 | 谁在干活 | 适合谁 | 现在做不做 |
| --- | --- | --- | --- | --- |
| **阶段一**：让 AI 助手（CodeBuddy）指挥跑 | 要 | AI 帮你敲命令、看报错、报结果 | 第一次用、想省事 | **现在先做这个** |
| **阶段二**：把命令做成「双击就跑」的脚本 | 不要 | 你自己双击一下 | 已经熟悉流程、要批量跑 | 阶段一跑顺了就做 |
| **阶段三**：做成打包好的 exe / 一键界面 | 不要 | 双击图标 | 给完全不懂电脑的人用 | 以后再考虑 |

为什么这个顺序？阶段一最快，出问题 AI 会直接帮你改；
等你把「命令长什么样、结果怎么看」摸清了，再固化成双击脚本，那时就不需要网络和 AI 了。

## 5. 准备工作（只做一次，以后都不用再装）

### 5.1 装 Python（必须有）

- 必须用 **Python 3.12**（3.13 的人脸库不稳定）。
- 建议装在非系统盘，例如 `E:\Tools\Python312`，别占 C 盘。
- 本机已经装好的路径：`E:\Tools\Python312\python.exe`
- 装完后验证：命令行输入下面这行，能显示 `Python 3.12.x` 就算成功：

```bash
E:\Tools\Python312\python.exe --version
```

### 5.2 装 FFmpeg（必须有，负责抽画面和读字幕）

- Windows 上下载「静态版」，解压到 `E:\Tools\ffmpeg`（解压后应存在 `E:\Tools\ffmpeg\bin\ffmpeg.exe`）。
- **不需要**加环境变量，程序会自己去这些地方找：`E:\Tools\ffmpeg\bin` → `C:\ffmpeg\bin` → `D:\ffmpeg\bin` → `C:\Program Files\ffmpeg\bin` → `D:\Program Files\ffmpeg\bin` → 系统 PATH。

### 5.3 装工具依赖（必须有，联网一次）

在工具目录下执行（只有这一次需要联网）：

```bash
cd /d "D:\03_Knowledge\intimate\Video Cut"
E:\Tools\Python312\python.exe -m pip install -r requirements.txt
```

### 5.4 装一个 AI 助手（阶段一用，可选）

- 推荐 **CodeBuddy**（本项目就是用它跑通的）：能读你本地的文件夹、帮你执行命令、还能直接看生成的图片。
- 也可以用 **VS Code** + Python 插件 + 任意 AI 插件。
- 关键只有一条：**要能把 `Video Cut` 这个文件夹打开成「工作区」**，AI 才能读到代码、执行命令。

### 5.5 一分钟自检

```bash
cd /d "D:\03_Knowledge\intimate\Video Cut"
E:\Tools\Python312\python.exe main.py --help
```

能看到一堆参数说明，就说明 Python、依赖、脚本都是好的；看不到就回到 5.1~5.3 检查。

## 6. 文件怎么摆（一分钟搞定）

规则只有一条：**一集视频放一个文件夹**，工具会自动在同级生成 `文件夹名 + "-pc"` 作为输出目录。

```
D:\03_Knowledge\intimate\
├── Video Cut\                     ← 工具本体（代码、模型、样式基线）
└── Love in the big City\          ← 剧集素材
    ├── 03\                        ← 第 3 集（放该集视频）
    │   └── 03-pc\                 ← 跑完自动出现：图片 + _index.csv + _report.txt
    ├── 04\   └── 04-pc\
    ├── 05\   └── 05-pc\
    └── 素材3.0-3.2图片\            ← 参考素材（用来统计台词样式，不要去动）
```

- 输出目录**不用自己建**，第一次跑会自动生成。
- 没下载完的文件（`.qkdownloading` / `.part` 等）会被自动跳过，报告里会写明；下载完整了再跑一次就行。

## 7. 阶段一：让 AI 助手帮你跑通（需要联网）

### 7.1 第一步：打开正确的工作区

在 AI 助手里「打开文件夹」，选 **`D:\03_Knowledge\intimate\Video Cut`**（注意：是工具目录，不是剧集目录）。

> 你会看到：AI 能列出 `main.py`、`video_tool` 等文件，说明它已经读到工具代码了。

### 7.2 第二步：先只跑 3 句，看效果（不要一上来跑整集）

把下面这段话原样发给 AI（不用记参数）：

```text
请用 E:\Tools\Python312\python.exe 运行本目录的 main.py，
输入目录 D:\03_Knowledge\intimate\Love in the big City\05，
先只处理前 3 句让我看效果，跑完把生成的图片路径和 _report.txt 的内容告诉我。
```

> 你会看到：AI 回报「成功 3 张」，并给出 3 个图片路径。打开图片检查：
> ① 台词位置、字号是否顺眼；② 人物是不是睁着眼；③ 台词和画面是否说得通。

> 说明：只跑 3 句时，报告里的「验收结论」会写「抽样试跑」，这是正常的——
> 因为它还没有覆盖整集，不代表出错。

### 7.3 第三步：确认没问题后，跑整集（重建）

```text
确认样式没问题，请对 05 集全量重跑：加 --clean 先清空历史图片，
跑完把报告里的成功数、失败数、缺失数、眼睛状态统计报给我。
```

> 你会看到：一集大约 3~6 分钟（300~400 句）。AI 应该用「后台运行 + 日志文件」的方式启动，
> 否则界面会一直卡着等。跑完报告里应满足：**成功数 = 台词总句数**、**失败 0 句**、**缺失 0 句**。

### 7.4 必须补上的一句话：让它理解剧情（很重要）

只按台词截，会漏掉「没有台词但承载剧情」的画面。把下面这段话一起交代给 AI：

```text
在执行视频截图时，需要深度理解视频中具体场景和台词的核心表达意图，
不仅截取直接出现台词字幕的画面，也要截取作为前文台词或情节延伸的关键画面。

例如第四集中，高煐妈妈在病床上让高煐把旁边抽屉里黄色的东西拿给她，
之后画面停留在高煐之前与卢英洙万圣节在夜店的合照上——
此类未直接出现当前台词、但承载前文情感或剧情延续的画面，
也应按其场景内涵识别并输出截图到对应文件夹中。
```

它会带来两条产出：

1. **工具自动出的图**：每句台词一张、画面与台词同时对得上、样式统一（基础盘）；
2. **补充截的图**：合影、物件特写、空镜、人物反应镜头等「没台词但重要」的画面（由 AI 结合前后文补抓）。

### 7.5 第四步：一集一集来，别一次全跑

- 一集跑完先验收（第 9 节），确认后再让它跑下一集。
- 想中途停下就这样说：`先只跑 05，跑完停下等我确认，不要自动往下跑 06。`

## 8. 阶段二：改成「不用网、不用 AI」的离线运行

等阶段一跑顺了，把命令固化成脚本，以后双击即可，**完全不需要网络**。

### 8.1 做一个双击就跑的脚本

在工具目录里新建一个文本文件，改名成 `跑05集.bat`（内容按你的集号改一行就行）：

```bat
@echo off
chcp 65001 >nul
cd /d "D:\03_Knowledge\intimate\Video Cut"
set PYTHONIOENCODING=utf-8
"E:\Tools\Python312\python.exe" -u main.py --input "D:\03_Knowledge\intimate\Love in the big City\05" --clean > _run05.log 2> _run05.err
echo.
echo 跑完了，请看 D:\03_Knowledge\intimate\Love in the big City\05\05-pc\_report.txt
pause
```

参数意思（不用记，照抄即可）：

| 参数 | 意思 |
| --- | --- |
| `--input "..."` | 处理哪个文件夹（一集的目录） |
| `--clean` | 先清空这一集的历史图片再重跑（换过样式时必加） |
| `--eye-refine half` | 半闭眼的句子也自动多抓几张再挑一次（默认只救「整句都闭眼」的句子） |
| `--max-cues 3` | 只跑前 3 句（试跑用，正式跑不要加） |
| `> _run05.log` | 把过程记录到日志文件，方便看 |

### 8.2 双击运行

双击 `跑05集.bat` 后，会弹出一个黑窗口开始跑；跑完窗口会停住等你按键。

> 你会看到：黑窗口里有进度文字；正常会有一些 `WARNING` 黄字，那是 AI 库的噪音，不影响结果。

### 8.3 检查结果

打开 `05\05-pc\_report.txt`，按第 9 节的三条对照看一遍；确认无误再双击下一集的脚本。

> 小提示：这些 `_run05.log` / `_run05.err` 只是过程记录，确认结果没问题后可以删掉。

## 9. 跑完怎么看结果（三条对照法）

打开 `xx-pc` 文件夹，看三样：

### ① 看 `_report.txt`（成绩单）

必须满足：

- `成功出图` 的数字 = `台词总句数`
- `处理失败` = 0
- `完整性校验 → 发现缺失` = 0
- `逐集验收 → 结论` 写的是「**通过**」（正式整集跑才会这么写）

### ② 看 `_index.csv`（台账）

- 行数（不含表头）应该和图片张数一样多；
- **眼睛状态**列：`睁开` 是理想结果，`半闭` 有点可惜，`闭合` 说明整句都没挑到更好的帧（越少越好）；空着表示这句画面里没有人；
- `说话状态`列：`正在说话` / `闭嘴` / `张口过大`；
- `是否降级`列：`是` 表示这句画面里没人或画面很空（空镜、物件特写、字幕卡），**属于正常保留，不是失败**。

### ③ 抽 2 张图眼看一遍

- 抽一张「**有人脸、眼睛状态=睁开**」的：确认人物神态自然、台词印得清楚；
- 抽一张「**无人像**」的：确认空镜/字幕卡的台词位置也正常。

### 9.1 本剧集已经跑完的记录（真实数据）

| 集 | 输出目录 | 台词句数 | 成功出图 | 降级 | 失败 | 缺失 | 耗时 | 结论 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| S01E03 | `03\03-pc` | 390 | 390 | 147 | 0 | 0 | 339 秒 | 通过（重建清理 392 个历史文件） |
| S01E04 | `04\04-pc` | 353 | 353 | 185 | 0 | 0 | 221 秒 | 通过（原目录为空，无需清理） |
| S01E05 | `05\05-pc` | — | — | — | — | — | — | 待生成（用户确认后执行） |
| S01E06~08 | — | — | — | — | — | — | — | 视频还没下载完，下载完整后按同一规则补做 |

两集共同点：**图片张数 = 台账行数 = 台词句数**，无重复句序、无失败、无缺失。

### 9.2 台词样式是从哪来的

- 基线文件：`Video Cut\style_profile.json`
- 来源：`Love in the big City\素材3.0-3.2图片`（人工精修过的参考图），命中 230 张样本
- 测量的量：台词高度占画面高度的比例、描边宽度、台词离画面底部的距离、行距
- 换参考素材后要重新统计时执行：`E:\Tools\Python312\python.exe tools\analyze_reference.py`
- 文件丢了也不会崩：会自动用内置默认值，报告里会写明当前用的是哪个来源

## 10. 常见问题（对着现象找）

| 现象 | 原因 | 怎么办 |
| --- | --- | --- |
| 报错「找不到字幕 / 没有台词」 | 片源没有文字型软字幕（只有硬字幕或图形字幕） | 换片源；本工具不做 OCR |
| 报告写「跳过未下载完成」 | 目录里有 `.qkdownloading` / `.part` 文件 | 等下载完整再跑一次 |
| 图片数比台词数少 | 极少数句子处理失败 | 直接再跑一次，它会自动补跑缺的句子 |
| 换过台词样式后，新旧图片混在一起 | 老图片还在目录里 | 加 `--clean` 重跑该集（会先清空这一集的图） |
| 中文显示成方块 | 系统缺中文字体 | 装微软雅黑，或把 `msyh.ttc` 放到 `C:\Windows\Fonts` |
| 看不到进度 | 后台跑的，看不到实时进度 | 看输出目录里图片数量在不在增加；或看 `_run05.log` |
| 满屏 WARNING / 黄字 | 人脸库的正常噪音 | 忽略，只看报告里的汇总 |
| 想确认哪几句「眼睛是闭的」 | 需要人工复查 | 在 `_index.csv` 里筛「眼睛状态 = 闭合 / 半闭」，对着图看 |
| 半闭眼的图偏多 | 候选帧抓得太少 | 试跑时加 `--candidate-max 15` 让它多抓几张再挑 |
| 报告里仍有「眼睛闭合」 | 整句候选都没睁眼，重采样也没救回来 | 属剧情动作（低头、闭眼喝水等）；想更严格可加 `--eye-refine half` 重跑该集 |

## 11. 术语对照表（看不懂就查这里）

| 文档里出现的词 | 大白话意思 |
| --- | --- |
| 台词 / 字幕轨 / 软字幕 | 视频里自带的文字字幕（能提取成文字，不是画在画面上的） |
| 候选帧 | 一句话的时间内抓到的多张画面，从中挑一张 |
| 眼睛状态（睁开/半闭/闭合） | 画面里最主要人物的眼睛睁得怎么样；半闭包含「一只眼睁一只眼眯」 |
| 说话状态（正在说话/闭嘴/张口过大） | 主要人物嘴巴的开合情况，用来辅助判断神态 |
| 降级 | 这句话的画面里没人（空镜、背影、远景、字幕卡），图照出，只是标注一下 |
| 完整性校验 | 跑完后自动对账：台词句数、图片张数、台账行数是不是一样 |
| 闭眼重采样 | 整句候选都闭眼时，自动把这一句的候选画面多抓一倍多、再挑一次 |
| 断点续跑 | 已经生成过的图片自动跳过，中断了再跑不会重复做 |
| 重建（`--clean`） | 先清掉这一集的历史图片和旧台账，再从零跑一遍 |
| 样式基线 | 从参考素材里统计出来的「台词该多大、多粗、离底边多远」的一组数值 |
| FFmpeg | 一个负责读视频、抽画面的免费小工具，程序在后台用它 |
| mediapipe 人脸模型 | 用来判断人脸、眼睛、嘴巴状态的本地小模型文件（约 3.5 MB） |

## 12. 一集从零到交付的完整清单

- [ ] 1. 环境已装好（第 5 节，一次性；`main.py --help` 能跑出说明）
- [ ] 2. 视频已放在 `Love in the big City\05\` 这样的单集文件夹里，且下载完整
- [ ] 3. 用 AI 助手打开 `Video Cut` 工作区（第 7.1 节）
- [ ] 4. 让 AI 跑「前 3 句」试跑，看图确认样式和人物眼睛（第 7.2 节）
- [ ] 5. 让 AI 跑整集（加 `--clean`），并要它报回成功/失败/缺失/眼睛统计（第 7.3 节）
- [ ] 6. 让 AI 按剧情补抓「没台词但重要」的画面（第 7.4 节）
- [ ] 7. 三条对照法验收：成绩单、台账、抽两张图（第 9 节）
- [ ] 8. 验收通过后，再做第二步的离线脚本，方便以后一键重跑（第 8 节）
- [ ] 9. 删除临时日志 `_run05.log` / `_run05.err`，收工

---

相关文档：工具的参数细节与实现说明见同目录 `README.md`。
